# Пункт 7.2: интеграционные тесты N+1 детекции

## Как реализовано
Добавлены интеграционные запросы к schema/executor с проверкой реального `profiling.nPlusOne` payload.

## Основные файлы
- `HotChocolate/Core/test/Execution.Tests/DependencyInjection/RequestExecutorBuilderExtensions_ExecutionProfiler.Tests.cs`

## Ключевые цитаты
```csharp
var result = (await executor.ExecuteAsync("{ users(count: 4) { profileName } }"))
    .ExpectOperationResult();
```

```csharp
var nPlusOne = GetNPlusOneExtension(profiling);
var issue = GetNPlusOneIssue(nPlusOne, "users[].profileName");

Assert.Equal(1, GetIntValue(nPlusOne, "issueCount"));
Assert.Equal(4, GetIntValue(issue, "occurrences"));
```

```csharp
Assert.True(profiling.ContainsKey("nPlusOne"));
Assert.False(profiling.ContainsKey("fields")); // для NPlusOneOnly
```

