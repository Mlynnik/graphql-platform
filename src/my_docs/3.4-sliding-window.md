# Пункт 3.4: скользящее окно (последние N запросов / временной интервал)

## Как реализовано
Сэмплы запросов хранятся в очереди. При каждом добавлении и при чтении snapshot вызывается `TrimWindow`, который удаляет устаревшие записи:
- по времени (`SlidingWindowDuration`)
- по лимиту количества (`SlidingWindowMaxRequests`)

## Основные файлы
- `HotChocolate/Core/src/Types/Execution/Profiling/ExecutionProfilerSlidingWindowAggregator.cs`
- `HotChocolate/Core/src/Types/Execution/Profiling/ExecutionProfilerOptions.cs`
- `HotChocolate/Core/test/Execution.Tests/DependencyInjection/RequestExecutorBuilderExtensions_ExecutionProfiler.Tests.cs`

## Ключевые цитаты
```csharp
_samples.Enqueue(sample);
TrimWindow(sample.CapturedAtUtc);
```

```csharp
if (_options.SlidingWindowDuration > TimeSpan.Zero)
{
    var minTimestamp = nowUtc - _options.SlidingWindowDuration;
    while (_samples.Count > 0 && _samples.Peek().CapturedAtUtc < minTimestamp)
    {
        _samples.Dequeue();
    }
}
```

```csharp
if (_options.SlidingWindowMaxRequests > 0)
{
    while (_samples.Count > _options.SlidingWindowMaxRequests)
    {
        _samples.Dequeue();
    }
}
```

## Подтверждение тестами
Тесты:
- `ExecuteAsync_Should_EvictOldRequests_When_SlidingWindowMaxRequestsExceeded`
- `SlidingWindowAggregator_Should_EvictRequestsOutsideConfiguredDuration`

