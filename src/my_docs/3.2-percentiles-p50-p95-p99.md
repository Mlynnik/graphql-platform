# Пункт 3.2: перцентили p50/p95/p99

## Как реализовано
Перцентили считаются на отсортированном списке сэмплов длительности в `DurationAccumulator`.

## Основные файлы
- `HotChocolate/Core/src/Types/Execution/Profiling/ExecutionProfilerSlidingWindowAggregator.cs`
- `HotChocolate/Core/test/Execution.Tests/DependencyInjection/RequestExecutorBuilderExtensions_ExecutionProfiler.Tests.cs`

## Ключевые цитаты
```csharp
var ordered = new List<long>(_samples);
ordered.Sort();
```

```csharp
["p50Ns"] = GetPercentile(ordered, 0.50d),
["p95Ns"] = GetPercentile(ordered, 0.95d),
["p99Ns"] = GetPercentile(ordered, 0.99d)
```

```csharp
var rank = (int)Math.Ceiling(orderedValues.Count * percentile) - 1;
...
return orderedValues[rank];
```

## Подтверждение тестами
Тест:
- `SlidingWindowAggregator_Should_CalculatePercentiles_When_CollectingFieldDurations`

